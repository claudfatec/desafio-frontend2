// @flow 
import { List } from '@material-ui/core';
import * as React from 'react';
type Props = {
};
export const Titulo = (props: Props) => {
    return (
        <div>
            <List>
                listaPosts.forEach.li.innerText();
            </List>
        </div>
    );
};